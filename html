 st.title("Optimal traffic route prediction for shuttle service")
    html_temp = """
    <div style="background:#025246 ;padding:10px">
    <h2 style="color:white;text-align:center;"> Optimal traffic route prediction App </h2>
    </div>
    """
    st.markdown(html_temp, unsafe_allow_html = True)
